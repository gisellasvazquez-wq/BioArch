<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BIOARCH — Estudio de Arquitectura</title>
  <meta name="description" content="Estudio de arquitectura que conecta la sabiduría ancestral con la tecnología emergente y el impacto ambiental." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Outfit:wght@200;300;400;500&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --cosmos:  #0d1520;
      --deep:    #111c2e;
      --mid:     #1a2744;
      --gold:    #c9a84c;
      --gold-lt: #e2c97e;
      --sage:    #5a7a51;
      --sand:    #d4c4a8;
      --cream:   #f0ead8;
      --txt:     #e8e0d0;
      --txt-dim: #8a8070;
      --serif:   'Cormorant Garamond', Georgia, serif;
      --sans:    'Outfit', sans-serif;
    }

    html { scroll-behavior: smooth; }
    body {
      background: var(--cosmos);
      color: var(--txt);
      font-family: var(--sans);
      font-weight: 300;
      overflow-x: hidden;
      cursor: none;
    }

    /* ── CURSOR ── */
    .cursor {
      position: fixed; top: 0; left: 0; z-index: 9999;
      width: 10px; height: 10px;
      background: var(--gold);
      border-radius: 50%;
      pointer-events: none;
      transform: translate(-50%,-50%);
      transition: width .2s, height .2s, opacity .2s;
      mix-blend-mode: screen;
    }
    .cursor-ring {
      position: fixed; top: 0; left: 0; z-index: 9998;
      width: 36px; height: 36px;
      border: 1px solid rgba(201,168,76,.4);
      border-radius: 50%;
      pointer-events: none;
      transform: translate(-50%,-50%);
      transition: transform .18s ease;
    }

    /* ── NAV ── */
    nav {
      position: fixed; top: 0; left: 0; right: 0;
      z-index: 200;
      display: flex; align-items: center; justify-content: space-between;
      padding: 1.4rem 3rem;
      transition: background .4s, backdrop-filter .4s;
    }
    nav.scrolled {
      background: rgba(13,21,32,.92);
      backdrop-filter: blur(14px);
    }
    .nav-logo {
      font-family: var(--serif);
      font-size: 1.5rem;
      font-weight: 300;
      letter-spacing: .28em;
      color: var(--gold);
      text-decoration: none;
    }
    .nav-logo span {
      color: var(--sand);
      font-style: italic;
      font-size: .72rem;
      display: block;
      letter-spacing: .18em;
      margin-top: -2px;
    }
    nav ul { list-style: none; display: flex; gap: 2.4rem; }
    nav ul a {
      font-size: .7rem; font-weight: 400;
      letter-spacing: .18em; text-transform: uppercase;
      color: var(--txt-dim); text-decoration: none;
      transition: color .3s;
    }
    nav ul a:hover { color: var(--gold); }

    /* ── HAMBURGER (mobile) ── */
    .hamburger {
      display: none;
      flex-direction: column; gap: 5px;
      background: none; border: none; cursor: none; padding: 4px;
    }
    .hamburger span {
      display: block; width: 22px; height: 1px;
      background: var(--sand); transition: .3s;
    }
    .hamburger.open span:nth-child(1) { transform: rotate(45deg) translate(4px,4px); }
    .hamburger.open span:nth-child(2) { opacity: 0; }
    .hamburger.open span:nth-child(3) { transform: rotate(-45deg) translate(4px,-4px); }

    /* ── HERO ── */
    #hero {
      position: relative;
      height: 100vh; min-height: 600px;
      display: flex; flex-direction: column;
      justify-content: flex-end;
      padding: 0 6rem 8rem;
      overflow: hidden;
    }

    /* Parallax image */
    .hero-img {
      position: absolute; inset: 0;
      background: url('https://images.unsplash.com/photo-1512917774080-9991f1c4c750?w=1800&q=80') center/cover no-repeat;
      transform: scale(1.08);
      animation: heroZoom 18s ease-out forwards;
    }
    @keyframes heroZoom { to { transform: scale(1); } }

    .hero-overlay {
      position: absolute; inset: 0;
      background: linear-gradient(
        160deg,
        rgba(8,13,22,.75) 0%,
        rgba(13,21,32,.55) 40%,
        rgba(8,13,22,.82) 100%
      );
    }

    /* Thin gold line — architectural detail */
    .hero-line {
      position: absolute;
      left: 6rem; top: 0; bottom: 0;
      width: 1px;
      background: linear-gradient(to bottom, transparent 10%, rgba(201,168,76,.35) 40%, rgba(201,168,76,.35) 60%, transparent 90%);
    }

    .hero-eyebrow {
      position: relative;
      font-size: .68rem; font-weight: 400;
      letter-spacing: .35em; text-transform: uppercase;
      color: var(--gold);
      display: flex; align-items: center; gap: 1rem;
      margin-bottom: 1.4rem;
      opacity: 0; animation: fadeUp .8s .3s forwards;
    }
    .hero-eyebrow::before {
      content: ''; width: 36px; height: 1px; background: var(--gold);
    }

    .hero-title {
      position: relative;
      font-family: var(--serif);
      font-size: clamp(3.2rem, 7.5vw, 6.5rem);
      font-weight: 300;
      line-height: .98;
      letter-spacing: .05em;
      color: var(--cream);
      margin-bottom: .5rem;
      opacity: 0; animation: fadeUp .9s .5s forwards;
    }
    .hero-title em { display: block; font-style: italic; color: var(--gold); }

    .hero-sub {
      position: relative;
      font-family: var(--serif);
      font-size: clamp(.95rem, 1.8vw, 1.25rem);
      font-style: italic; font-weight: 300;
      color: var(--sand);
      max-width: 520px; line-height: 1.7;
      margin: 1.4rem 0 2.6rem;
      opacity: 0; animation: fadeUp .9s .7s forwards;
    }

    .hero-cta {
      position: relative;
      display: flex; gap: 1.2rem; align-items: center;
      opacity: 0; animation: fadeUp .9s .9s forwards;
    }
    .btn-primary {
      font-family: var(--sans); font-size: .7rem; font-weight: 400;
      letter-spacing: .2em; text-transform: uppercase;
      color: var(--cosmos); background: var(--gold);
      border: none; padding: .85rem 2.2rem;
      text-decoration: none; cursor: none;
      transition: background .3s, transform .2s;
    }
    .btn-primary:hover { background: var(--gold-lt); transform: translateY(-1px); }
    .btn-ghost {
      font-family: var(--sans); font-size: .7rem; font-weight: 400;
      letter-spacing: .2em; text-transform: uppercase;
      color: var(--sand); border: 1px solid rgba(212,196,168,.3);
      padding: .85rem 2.2rem; text-decoration: none; cursor: none;
      transition: border-color .3s, color .3s;
    }
    .btn-ghost:hover { border-color: var(--gold); color: var(--gold); }

    /* Scroll hint */
    .hero-scroll {
      position: absolute; bottom: 2.5rem; right: 3rem;
      display: flex; flex-direction: column; align-items: center; gap: .5rem;
      font-size: .58rem; letter-spacing: .2em; text-transform: uppercase;
      color: rgba(212,196,168,.4);
      opacity: 0; animation: fadeUp 1s 1.2s forwards;
    }
    .scroll-line {
      width: 1px; height: 46px;
      background: linear-gradient(to bottom, rgba(201,168,76,.6), transparent);
      animation: scrollPulse 2.2s ease-in-out infinite;
    }
    @keyframes scrollPulse { 0%,100%{opacity:.4} 50%{opacity:1} }
    @keyframes fadeUp { from{opacity:0;transform:translateY(20px)} to{opacity:1;transform:translateY(0)} }

    /* ── FULL DIVIDER ── */
    .full-divider {
      height: 1px;
      background: linear-gradient(to right, transparent, rgba(201,168,76,.2), transparent);
      margin: 0;
    }

    /* ── SECTION BASE ── */
    section { padding: 7rem 3rem; }
    .container { max-width: 1100px; margin: 0 auto; }

    .section-label {
      font-size: .65rem; font-weight: 400;
      letter-spacing: .3em; text-transform: uppercase;
      color: var(--gold); margin-bottom: 1rem;
      display: flex; align-items: center; gap: 1rem;
    }
    .section-label::after {
      content: ''; flex: 1; max-width: 40px; height: 1px; background: var(--gold);
    }
    .section-title {
      font-family: var(--serif);
      font-size: clamp(2rem, 4vw, 3.2rem);
      font-weight: 300; line-height: 1.15;
      color: var(--cream); margin-bottom: 1.4rem;
    }
    .section-title em { font-style: italic; color: var(--gold); }
    .section-body {
      font-size: .95rem; font-weight: 300;
      line-height: 1.85; color: var(--txt-dim); max-width: 640px;
    }

    /* ── FILOSOFIA ── */
    #filosofia { background: var(--cosmos); }
    .filosofia-grid {
      display: grid; grid-template-columns: 1fr 1fr;
      gap: 5rem; align-items: center; margin-top: 4rem;
    }
    .filosofia-quote {
      font-family: var(--serif);
      font-size: clamp(1.15rem, 2vw, 1.55rem);
      font-style: italic; font-weight: 300;
      color: var(--cream); line-height: 1.65;
      padding-left: 2rem;
      border-left: 2px solid var(--gold);
    }
    .filosofia-text p {
      font-size: .92rem; font-weight: 300;
      line-height: 1.85; color: var(--txt-dim); margin-bottom: 1.2rem;
    }

    /* ── IMAGE STRIP ── */
    .img-strip {
      display: grid; grid-template-columns: repeat(3,1fr); gap: 3px;
      overflow: hidden;
    }
    .img-strip-item {
      position: relative; height: 320px; overflow: hidden;
    }
    .img-strip-item img {
      width: 100%; height: 100%; object-fit: cover;
      transition: transform .8s ease;
      filter: brightness(.75) saturate(.7);
    }
    .img-strip-item:hover img { transform: scale(1.06); filter: brightness(.9) saturate(.85); }
    .img-strip-caption {
      position: absolute; bottom: 1rem; left: 1.2rem;
      font-size: .6rem; letter-spacing: .2em; text-transform: uppercase;
      color: rgba(212,196,168,.6);
    }

    /* ── PILARES ── */
    #pilares { background: var(--deep); }
    .pilares-grid {
      display: grid; grid-template-columns: repeat(3,1fr);
      gap: 1px; margin-top: 4rem;
      background: rgba(201,168,76,.08);
    }
    .pilar {
      background: var(--deep); padding: 3rem 2.2rem;
      position: relative; transition: background .4s;
    }
    .pilar:hover { background: rgba(26,39,68,.8); }
    .pilar::before {
      content: attr(data-num);
      position: absolute; top: 2.2rem; right: 2rem;
      font-family: var(--serif); font-size: 4rem; font-weight: 300;
      color: rgba(201,168,76,.07); line-height: 1;
    }
    .pilar-icon { width: 40px; height: 40px; margin-bottom: 1.5rem; }
    .pilar h3 {
      font-family: var(--serif); font-size: 1.3rem; font-weight: 400;
      color: var(--cream); margin-bottom: .4rem; line-height: 1.25;
    }
    .pilar .pilar-tag {
      font-size: .62rem; letter-spacing: .2em; text-transform: uppercase;
      color: var(--gold); margin-bottom: 1rem;
    }
    .pilar p { font-size: .88rem; font-weight: 300; line-height: 1.8; color: var(--txt-dim); }

    /* ── VISION ── */
    #vision { background: var(--cosmos); }
    .vm-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; margin-top: 4rem; }
    .vm-card {
      padding: 2.8rem; border: 1px solid rgba(201,168,76,.15); position: relative;
      transition: border-color .4s;
    }
    .vm-card:hover { border-color: rgba(201,168,76,.4); }
    .vm-card::before {
      content: ''; position: absolute; top: 0; left: 0;
      width: 40px; height: 2px; background: var(--gold);
    }
    .vm-card h3 {
      font-size: .65rem; letter-spacing: .3em; text-transform: uppercase;
      color: var(--gold); margin-bottom: 1.2rem;
    }
    .vm-card p {
      font-family: var(--serif); font-size: 1.05rem; font-weight: 300;
      line-height: 1.8; color: var(--sand);
    }
    .gold-divider { width: 60px; height: 1px; background: var(--gold); margin: 3rem auto; }

    /* ── COMO TRABAJAMOS ── */
    #como { background: var(--deep); }

    .como-image-block {
      position: relative; margin-top: 4rem;
      display: grid; grid-template-columns: 1.2fr 1fr; gap: 3rem; align-items: center;
    }
    .como-img-wrap {
      position: relative; height: 480px; overflow: hidden;
    }
    .como-img-wrap img {
      width: 100%; height: 100%; object-fit: cover;
      filter: brightness(.7) saturate(.65);
      transition: filter .6s, transform .8s;
    }
    .como-img-wrap:hover img { filter: brightness(.85) saturate(.8); transform: scale(1.03); }
    /* Gold frame detail */
    .como-img-wrap::before {
      content: '';
      position: absolute; inset: 14px; z-index: 1;
      border: 1px solid rgba(201,168,76,.2);
      pointer-events: none;
    }

    .como-content { padding: 1rem 0; }
    .como-content .section-label { margin-bottom: 1rem; }
    .como-content h2 { margin-bottom: 1.5rem; }
    .como-steps { margin-top: 2rem; display: flex; flex-direction: column; gap: 1.6rem; }
    .como-step { display: flex; gap: 1.2rem; align-items: flex-start; }
    .step-num {
      flex-shrink: 0;
      font-family: var(--serif); font-size: 1.6rem; font-weight: 300;
      color: rgba(201,168,76,.35); line-height: 1; width: 2rem;
    }
    .step-text h4 {
      font-family: var(--serif); font-size: 1.05rem; font-weight: 400;
      color: var(--cream); margin-bottom: .3rem;
    }
    .step-text p { font-size: .85rem; font-weight: 300; line-height: 1.75; color: var(--txt-dim); }

    /* ── CONTACTO ── */
    #contacto { background: var(--cosmos); position: relative; overflow: hidden; }

    /* Background image behind contact */
    .contacto-bg {
      position: absolute; inset: 0; z-index: 0;
      background: url('https://images.unsplash.com/photo-1600585154340-be6161a56a0c?w=1600&q=80') center/cover no-repeat;
      filter: brightness(.2) saturate(.4);
    }
    .contacto-inner { position: relative; z-index: 1; text-align: center; }

    #contacto .section-label {
      justify-content: center;
    }
    #contacto .section-label::after { display: none; }
    #contacto .section-label::before {
      content: ''; flex: 1; max-width: 40px; height: 1px; background: var(--gold);
    }

    .contact-heading {
      font-family: var(--serif);
      font-size: clamp(2.4rem, 5vw, 4rem);
      font-weight: 300; line-height: 1.1;
      color: var(--cream); margin-bottom: 1rem;
    }
    .contact-heading em { font-style: italic; color: var(--gold); }

    .contact-subtext {
      font-family: var(--serif);
      font-size: clamp(1rem, 1.8vw, 1.25rem);
      font-style: italic; font-weight: 300;
      color: var(--sand); max-width: 580px;
      margin: 0 auto 3rem; line-height: 1.7;
    }

    .contact-cards {
      display: flex; justify-content: center; gap: 2rem;
      flex-wrap: wrap; margin-bottom: 3.5rem;
    }
    .contact-card {
      display: flex; flex-direction: column; align-items: center; gap: .5rem;
      padding: 1.6rem 2.4rem;
      border: 1px solid rgba(201,168,76,.2);
      background: rgba(13,21,32,.6);
      backdrop-filter: blur(8px);
      transition: border-color .3s, background .3s;
      min-width: 200px;
    }
    .contact-card:hover { border-color: rgba(201,168,76,.5); background: rgba(26,39,68,.7); }
    .contact-card-label {
      font-size: .6rem; letter-spacing: .25em; text-transform: uppercase;
      color: var(--gold);
    }
    .contact-card-value {
      font-family: var(--serif); font-size: 1.05rem; font-weight: 300;
      color: var(--cream); text-decoration: none;
    }
    a.contact-card-value { cursor: none; transition: color .3s; }
    a.contact-card-value:hover { color: var(--gold-lt); }

    .contact-prompt {
      font-size: .82rem; font-weight: 300; letter-spacing: .04em;
      color: var(--txt-dim); max-width: 480px; margin: 0 auto;
      line-height: 1.85;
    }
    .contact-prompt strong { color: var(--sand); font-weight: 400; }

    /* ── FOOTER ── */
    footer {
      background: #060c14;
      padding: 2.2rem 3rem;
      display: flex; justify-content: space-between; align-items: center;
    }
    .footer-logo {
      font-family: var(--serif); font-size: 1.1rem; font-weight: 300;
      letter-spacing: .3em; color: rgba(201,168,76,.45);
    }
    .footer-copy { font-size: .66rem; color: rgba(138,128,112,.4); letter-spacing: .1em; }

    /* ── REVEAL ── */
    .reveal { opacity: 0; transform: translateY(22px); transition: opacity .7s ease, transform .7s ease; }
    .reveal.visible { opacity: 1; transform: translateY(0); }

    /* ── MOBILE ── */
    @media (max-width: 768px) {
      nav { padding: 1.1rem 1.5rem; }
      nav ul { display: none; }
      nav ul.open {
        display: flex; flex-direction: column; gap: 1.5rem;
        position: fixed; inset: 0; top: 60px;
        background: rgba(8,13,22,.97); backdrop-filter: blur(12px);
        align-items: center; justify-content: center;
        padding: 2rem;
      }
      nav ul.open a { font-size: 1rem; }
      .hamburger { display: flex; }
      #hero { padding: 0 1.5rem 5rem; }
      .hero-line { left: 1.5rem; }
      section { padding: 4.5rem 1.5rem; }
      .filosofia-grid, .vm-layout, .como-image-block { grid-template-columns: 1fr; gap: 2.5rem; }
      .pilares-grid { grid-template-columns: 1fr; }
      .img-strip { grid-template-columns: 1fr; }
      .img-strip-item { height: 220px; }
      .como-img-wrap { height: 280px; }
      .contact-cards { flex-direction: column; align-items: center; }
      footer { flex-direction: column; gap: .8rem; text-align: center; }
    }
  </style>
</head>
<body>

  <div class="cursor" id="cursor"></div>
  <div class="cursor-ring" id="cursorRing"></div>

  <!-- NAV -->
  <nav id="nav">
    <a href="#hero" class="nav-logo">
      BIOARCH
      <span>Estudio de Arquitectura</span>
    </a>
    <ul id="navLinks">
      <li><a href="#filosofia">Filosofía</a></li>
      <li><a href="#pilares">Pilares</a></li>
      <li><a href="#vision">Visión</a></li>
      <li><a href="#como">Cómo trabajamos</a></li>
      <li><a href="#contacto">Contacto</a></li>
    </ul>
    <button class="hamburger" id="hamburger" aria-label="Menú">
      <span></span><span></span><span></span>
    </button>
  </nav>

  <!-- ── HERO ── -->
  <section id="hero">
    <div class="hero-img"></div>
    <div class="hero-overlay"></div>
    <div class="hero-line"></div>

    <p class="hero-eyebrow">Buenos Aires · Argentina</p>
    <h1 class="hero-title">
      BIO<em>ARCH</em>
    </h1>
    <p class="hero-sub">
      Arquitectura que dialoga con el cosmos, respeta la tierra y abraza la tecnología del presente.
    </p>
    <div class="hero-cta">
      <a href="#filosofia" class="btn-primary">Conocer el estudio</a>
      <a href="#contacto" class="btn-ghost">Hablemos</a>
    </div>

    <div class="hero-scroll">
      <div class="scroll-line"></div>
      <span>Scroll</span>
    </div>
  </section>

  <!-- IMAGE STRIP -->
  <div class="img-strip">
    <div class="img-strip-item">
      <img src="https://images.unsplash.com/photo-1493397212122-2b85dda8106b?w=800&q=80" alt="Arquitectura monumental" loading="lazy" />
      <div class="img-strip-caption">Memoria ancestral</div>
    </div>
    <div class="img-strip-item">
      <img src="https://images.unsplash.com/photo-1545324418-cc1a3fa10c00?w=800&q=80" alt="Arquitectura bioclimática" loading="lazy" />
      <div class="img-strip-caption">Materia viva</div>
    </div>
    <div class="img-strip-item">
      <img src="https://images.unsplash.com/photo-1486325212027-8081e485255e?w=800&q=80" alt="Diseño contemporáneo" loading="lazy" />
      <div class="img-strip-caption">Tecnología consciente</div>
    </div>
  </div>

  <!-- ── FILOSOFIA ── -->
  <section id="filosofia">
    <div class="container">
      <p class="section-label reveal">Filosofía del Estudio</p>
      <h2 class="section-title reveal">La arquitectura <em>como puente</em><br>entre mundos</h2>

      <div class="filosofia-grid">
        <div class="filosofia-quote reveal">
          "Las grandes construcciones de la humanidad no fueron solo estructuras — fueron conversaciones con el cosmos, escritas en piedra, alineadas con los astros."
        </div>
        <div class="filosofia-text reveal">
          <p>Desde las pirámides de Guiza hasta los templos mayas, desde Göbekli Tepe hasta Angkor Wat, las civilizaciones ancestrales construyeron con una inteligencia que trascendía lo meramente técnico. Sus monumentos eran portales cargados de geometría, símbolo y sabiduría cósmica.</p>
          <p>Hoy, mientras la tecnología avanza sin pausa y la crisis ambiental exige respuestas urgentes, BIOARCH nace de una convicción: la arquitectura del futuro tiene raíces profundas. No como nostalgia — como memoria activa puesta al servicio del presente.</p>
        </div>
      </div>
    </div>
  </section>

  <div class="full-divider"></div>

  <!-- ── PILARES ── -->
  <section id="pilares">
    <div class="container">
      <p class="section-label reveal">Los Tres Pilares</p>
      <h2 class="section-title reveal">Lo que nos <em>define</em></h2>

      <div class="pilares-grid">

        <div class="pilar reveal" data-num="I">
          <svg class="pilar-icon" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="20" cy="20" r="18" stroke="#c9a84c" stroke-width=".8"/>
            <circle cx="20" cy="20" r="10" stroke="#c9a84c" stroke-width=".5" stroke-dasharray="2 2"/>
            <line x1="20" y1="2" x2="20" y2="38" stroke="#c9a84c" stroke-width=".6"/>
            <line x1="2" y1="20" x2="38" y2="20" stroke="#c9a84c" stroke-width=".6"/>
            <polygon points="20,5 22,18 20,15 18,18" fill="#c9a84c" opacity=".7"/>
          </svg>
          <h3>Dimensión Ancestral</h3>
          <div class="pilar-tag">Símbolo · Cosmos · Geometría</div>
          <p>Recuperamos el lenguaje constructivo de las grandes culturas: proporción universal, geometría sagrada, orientación astronómica y arte simbólico integrado en la materia.</p>
        </div>

        <div class="pilar reveal" data-num="II">
          <svg class="pilar-icon" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M20 36 C20 36 5 28 5 16 C5 8 12 4 20 4 C28 4 35 8 35 16 C35 28 20 36 20 36Z" stroke="#5a7a51" stroke-width=".8"/>
            <path d="M20 36 L20 12" stroke="#5a7a51" stroke-width=".6"/>
            <path d="M20 20 L10 14" stroke="#5a7a51" stroke-width=".4"/>
            <path d="M20 24 L30 18" stroke="#5a7a51" stroke-width=".4"/>
          </svg>
          <h3>Dimensión Presente</h3>
          <div class="pilar-tag">Ambiente · Biología · Territorio</div>
          <p>El impacto ambiental es eje central, no condición secundaria. Materiales de bajo carbono, sistemas bioclimáticos, integración del verde como estructura viva.</p>
        </div>

        <div class="pilar reveal" data-num="III">
          <svg class="pilar-icon" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
            <rect x="12" y="12" width="16" height="16" stroke="#c9a84c" stroke-width=".8"/>
            <circle cx="20" cy="20" r="4" stroke="#c9a84c" stroke-width=".6"/>
            <line x1="12" y1="20" x2="4" y2="20" stroke="#c9a84c" stroke-width=".6"/>
            <line x1="28" y1="20" x2="36" y2="20" stroke="#c9a84c" stroke-width=".6"/>
            <line x1="20" y1="12" x2="20" y2="4" stroke="#c9a84c" stroke-width=".6"/>
            <line x1="20" y1="28" x2="20" y2="36" stroke="#c9a84c" stroke-width=".6"/>
            <circle cx="4" cy="20" r="1.5" fill="#c9a84c"/>
            <circle cx="36" cy="20" r="1.5" fill="#c9a84c"/>
            <circle cx="20" cy="4" r="1.5" fill="#c9a84c"/>
            <circle cx="20" cy="36" r="1.5" fill="#c9a84c"/>
          </svg>
          <h3>Dimensión Emergente</h3>
          <div class="pilar-tag">IA · Tecnología · Futuro</div>
          <p>La inteligencia artificial como extensión del pensamiento proyectual: simular comportamientos ambientales, encontrar patrones en la geometría ancestral, personalizar espacios.</p>
        </div>

      </div>
    </div>
  </section>

  <div class="full-divider"></div>

  <!-- ── VISION & MISION ── -->
  <section id="vision">
    <div class="container">
      <p class="section-label reveal">Hacia Dónde Vamos</p>
      <h2 class="section-title reveal">Visión <em>&</em> Misión</h2>

      <div class="vm-layout">
        <div class="vm-card reveal">
          <h3>Visión</h3>
          <p>Un mundo donde los edificios no solo existen sin dañar al planeta, sino que activamente lo sanan. Donde los espacios elevan la conciencia de quienes los habitan. Donde la tecnología amplifica lo sagrado en lugar de desplazarlo.</p>
        </div>
        <div class="vm-card reveal">
          <h3>Misión</h3>
          <p>Crear proyectos arquitectónicos que integren de forma coherente lo ancestral, lo ambiental y lo tecnológico — recuperando el hilo de sabiduría que las culturas monumentales tejieron durante milenios y llevándolo al siglo XXI.</p>
        </div>
      </div>

      <div class="gold-divider"></div>

      <div style="text-align:center;" class="reveal">
        <p style="font-family:var(--serif);font-size:clamp(1.3rem,2.5vw,1.8rem);font-style:italic;color:var(--sand);max-width:700px;margin:0 auto;line-height:1.7;">
          "Construir es un acto sagrado.<br>Siempre lo fue."
        </p>
      </div>
    </div>
  </section>

  <div class="full-divider"></div>

  <!-- ── COMO TRABAJAMOS ── -->
  <section id="como">
    <div class="container">
      <div class="como-image-block">

        <div class="como-img-wrap reveal">
          <img src="https://images.unsplash.com/photo-1503387762-592deb58ef4e?w=900&q=80" alt="Proceso de diseño arquitectónico" loading="lazy" />
        </div>

        <div class="como-content">
          <p class="section-label reveal">Proceso</p>
          <h2 class="section-title reveal">Cómo <em>trabajamos</em></h2>
          <p class="section-body reveal">Cada proyecto comienza con una escucha profunda — del lugar, del cliente, de la historia del territorio. Después de eso, el diseño.</p>

          <div class="como-steps">
            <div class="como-step reveal">
              <div class="step-num">01</div>
              <div class="step-text">
                <h4>Lectura del lugar</h4>
                <p>Estudiamos el territorio: su orientación solar, su historia, sus materiales locales y los patrones simbólicos que emergen del entorno cultural.</p>
              </div>
            </div>
            <div class="como-step reveal">
              <div class="step-num">02</div>
              <div class="step-text">
                <h4>Diseño integrado</h4>
                <p>Integramos geometría, bioclimatismo y tecnología desde el primer trazo. La sustentabilidad no se agrega al final — es la estructura de todo.</p>
              </div>
            </div>
            <div class="como-step reveal">
              <div class="step-num">03</div>
              <div class="step-text">
                <h4>Materialización consciente</h4>
                <p>Cada decisión de material, escala y detalle responde a principios de bajo impacto, larga vida útil y relación armónica con quienes van a habitarlo.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── CONTACTO ── -->
  <section id="contacto">
    <div class="contacto-bg"></div>
    <div class="container contacto-inner">

      <p class="section-label reveal">Conectemos</p>
      <h2 class="contact-heading reveal">
        ¿Tenés un proyecto<br>en <em>mente?</em>
      </h2>
      <p class="contact-subtext reveal">
        Nos encanta arrancar desde una idea, una intuición, o incluso una sensación de lo que querés que ese espacio transmita. Escribinos.
      </p>

      <div class="contact-cards reveal">
        <div class="contact-card">
          <span class="contact-card-label">Email</span>
          <a href="mailto:gisella.svazquez@gmail.com" class="contact-card-value">gisella.svazquez@gmail.com</a>
        </div>
        <div class="contact-card">
          <span class="contact-card-label">Ubicación</span>
          <span class="contact-card-value">Buenos Aires, Argentina</span>
        </div>
      </div>

      <p class="contact-prompt reveal">
        Ya sea un proyecto de vivienda, un espacio comunitario, una consulta sobre diseño sustentable o simplemente ganas de conversar sobre arquitectura y territorio —
        <strong>todas las conversaciones son bienvenidas.</strong>
      </p>

    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="footer-logo">BIOARCH</div>
    <div class="footer-copy">© 2025 BIOARCH Estudio de Arquitectura · Buenos Aires, Argentina</div>
  </footer>

  <script>
    // ── Cursor ──
    const cursor = document.getElementById('cursor');
    const ring   = document.getElementById('cursorRing');
    let mx=0, my=0, rx=0, ry=0;
    document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });
    (function animCursor() {
      cursor.style.left = mx+'px'; cursor.style.top = my+'px';
      rx += (mx-rx)*.12; ry += (my-ry)*.12;
      ring.style.left = rx+'px'; ring.style.top = ry+'px';
      requestAnimationFrame(animCursor);
    })();
    document.querySelectorAll('a, button').forEach(el => {
      el.addEventListener('mouseenter', () => { cursor.style.width='20px'; cursor.style.height='20px'; cursor.style.opacity='.5'; });
      el.addEventListener('mouseleave', () => { cursor.style.width='10px'; cursor.style.height='10px'; cursor.style.opacity='1'; });
    });

    // ── Nav scroll ──
    const nav = document.getElementById('nav');
    window.addEventListener('scroll', () => nav.classList.toggle('scrolled', scrollY > 50), {passive:true});

    // ── Hamburger ──
    const ham   = document.getElementById('hamburger');
    const links = document.getElementById('navLinks');
    ham.addEventListener('click', () => {
      ham.classList.toggle('open');
      links.classList.toggle('open');
    });
    links.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
      ham.classList.remove('open'); links.classList.remove('open');
    }));

    // ── Reveal on scroll ──
    const io = new IntersectionObserver(entries => {
      entries.forEach((e, i) => {
        if (e.isIntersecting) {
          setTimeout(() => e.target.classList.add('visible'), i * 70);
          io.unobserve(e.target);
        }
      });
    }, { threshold: 0.1 });
    document.querySelectorAll('.reveal').forEach(el => io.observe(el));
  </script>
</body>
</html>